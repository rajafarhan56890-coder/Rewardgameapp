{
  "name": "coinvault-rewards-functions",
  "description": "Optional server-side functions for CoinVault Rewards. Recommended for production: moves balance-mutating writes off the client entirely for airtight security.",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.3.1",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "private": true
}
